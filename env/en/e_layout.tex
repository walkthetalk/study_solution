\setuppapersize[A4][A4]

\setuppagenumbering[
  state=start,
  way=bytext,
  alternative=doublesided,
  location=,%header,
  %left=the,
  %right=ok,
]
% don't use TAB, and no space between *reg=val*, and no empty line,
% or it will be ommitted
\setuplayout[
  % vertical
  top=20mm,
    topdistance=2mm,
      header=7mm,
        headerdistance=2mm,
        footerdistance=2mm,
      footer=7mm,
    bottomdistance=2mm,
  bottom=20mm,
  %topspace=22mm,%\dimexpr(\topheight + \topdistance),
  %bottomspace=22mm,%\dimexpr(\bottomheight + \bottomdistance),
  height=fit,
  % horizontal
  backspace=36mm,
    leftedge=10mm,
      leftedgedistance=2mm,
        leftmargin=20mm,
          leftmargindistance=2mm,
          rightmargindistance=2mm,
        rightmargin=32mm,
      rightedgedistance=2mm,
    rightedge=10mm,
  cutspace=36mm,
  %backspace=34mm,%\dimexpr(\leftedgewidth + \leftedgedistance + \leftmarginwidth + \leftmargindistance + 5mm),
  %cutspace=34mm,%\dimexpr(\rightedgewidth + \rightedgedistance + \rightmarginwidth + \rightmargindistance),
  width=fit,
  % misc
  location=middle,
  marking=on
]

% set the background clors
% NOTE: it is confilict with \show frame
\if 0
\setupbackgrounds[page][background=color,backgroundcolor=gray:1]
\setupbackgrounds[top, bottom][background=color,backgroundcolor=gray:1]
\setupbackgrounds[header][background=color,backgroundcolor=darkyellow]
\setupbackgrounds[footer][background=color,backgroundcolor=darkyellow]
\setupbackgrounds[text][leftedge,leftmargin,text,rightmargin,rightedge][background=color,backgroundcolor=white]
\fi
